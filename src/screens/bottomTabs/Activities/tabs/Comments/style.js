import {StyleSheet} from 'react-native';
import {colors} from '../../../../../utils/utils';

const styles = StyleSheet.create({
  main: {
    flex: 1,
    backgroundColor: colors.whiteColor,
    width: '100%',
    paddingHorizontal: '5%',
    alignSelf: 'center',
  },
  heading: {marginTop: '7.5%', marginBottom: '5%'},
});
export default styles;
